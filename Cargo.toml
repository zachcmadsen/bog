[package]
name = "bog"
version = "0.1.0"
edition = "2021"
exclude = ["roms/*.a65", "roms/*.lst"]

[dependencies]
bitflags = "2.0.0-rc.1"

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
